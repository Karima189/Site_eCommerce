{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block body %}

	{% if app.request.attributes.get('_route') == 'app_home' %}
		<div id="carouselVetements" class="carousel slide" data-ride="carousel" data-interval="2000">
			<div class="carousel_inner carousel_front">
				{% set firstItem = true %}
				{% for carousel in carouselImages %}
					<div class="carousel-item {{firstItem ? 'active'}}">
						<img src="{{asset('/img/' ~ carousel.image)}}" class="d-block w-100" class="image_front_carousel" alt="">
					</div>
					{% set firstItem = false %}
				{% endfor %}

			</div>
			<a class="carousel-control-prev" href="#carouselVetements" role="button" data-slide="prev">
				<span class="carousel-control-prev-icon" aria-hidden="true"></span>
				<span class="sr-only">Previous</span>
			</a>
			<a class="carousel-control-next" href="#carouselVetements" role="button" data-slide="next">
				<span class="carousel-control-next-icon" aria-hidden="true"></span>
				<span class="sr-only">Next</span>
			</a>
		</div>

		<h3>Notre séléction Make Up</h3>
		<div class="liste_produits">
			{% for produit in produits %}
				<div class="makeup_liste">
					<div>
						<img src="{{ asset('img/' ~ produit.image) }}" alt="{{ produit.description }}" width="400" height="400">
					</div>
					<h2 style="width:350px;margin-left:20px;">{{ produit.description }}</h2>
					<a href="{{ path('afficher_produit', {'id': produit.id}) }}" class="bouton_home">
						Voir le produit
					</a>
				</div>
			{% endfor %}

		</div>

		{# <div class="carousel">
															<div class="carousel-inner">
																{% for produit in produits %}
																	<div class="carousel-item">
																		<div>
																			<img src="{{ asset('img/' ~ produit.image) }}" alt="{{ produit.description }}" width="400" height="400">
																		</div>
																		<h2 style="width:350px;margin-left:20px;">{{ produit.description }}</h2>
																		<a href="{{ path('afficher_produit', {'id': produit.id}) }}" class="bouton_home">Voir le produit</a>
																	</div>
																{% endfor %}
															</div>
															<div class="carousel-controls">
																<button onclick="moveCarousel(-1)">Précédent</button>
																<button onclick="moveCarousel(1)">Suivant</button>
															</div>
														</div> #}


		{# <div class="flex-caroussel">
																									<button class="prev">❮</button>
																									<div class="carousel-container">
																										{% for produit in produits %}
																											<div class="card-flex">
																												<div>
																													<img src="{{ asset('img/' ~ produit.image) }}" alt="{{ produit.description }}">
																												</div>
																												<h2 style="width:350px;margin-left:20px;">{{ produit.description }}</h2>
																												<a href="{{ path('afficher_produit', {'id': produit.id}) }}" class="bouton_home">
																													Voir le produit
																												</a>
																											</div>
																										{% endfor %}
																									</div>
																									<button class="next">❯</button>
																								</div> #}


		<div class=" d-flex justify-content-around align-items-center">


			<h1 class="ecritureCarousel">Une vaste collection de vetements pour une élégance magnifique et une beauté impréssionnante</h1>

			<div id="carouselVetements" class="carousel slide" data-ride="carousel" data-interval="2000">
				<div class="carousel-inner">
					{% set firstItem = true %}
					{% for vetement in vetements %}
						<div class="carousel-item {{firstItem ? 'active'}}">
							<img src="{{asset('/img/' ~ vetement.image)}}" class="d-block w-100" alt="">
						</div>
						{% set firstItem = false %}
					{% endfor %}

				</div>
				<a class="carousel-control-prev" href="#carouselVetements" role="button" data-slide="prev">
					<span class="carousel-control-prev-icon" aria-hidden="true"></span>
					<span class="sr-only">Previous</span>
				</a>
				<a class="carousel-control-next" href="#carouselVetements" role="button" data-slide="next">
					<span class="carousel-control-next-icon" aria-hidden="true"></span>
					<span class="sr-only">Next</span>
				</a>
			</div>
			<h1 class="ecritureCarousel">Des différentes choix et styles exposés spécialement pour vous , toutes les gouts et toutes les envies!</h1>


		</div>
		<h3>Nos Promotions de Coffrets Bijoux
		</h3>
		{# <div class="d-flex justify-content-around" class="promo-bijoux">
																																								
																																								
																																											{% for produit in bijoux %}
																																												<div class="coffre1">
																																													<div>
																																														<img src="{{ asset('img/' ~ produit.image) }}" alt="{{ produit.description }}" width="400" height="400">
																																													</div>
																																													<h2 style="width:400px;margin-left:20px;">{{ produit.description }}</h2>
																																													<div class="promo1">
																																														OFFRE EXEPCIONNELLE
																																													</div>
																																													<div class="promo2">PROMO
																																													</div>
																																								
																																												</div>
																																											{% endfor %}
																																								
																																										</div> #}

		<div class="d-flex justify-content-around" class="promo-bijoux">
			{% for produit in bijoux %}
				<div class="coffre1">
					<a href="{{ path('afficher_produit', {'id': produit.id}) }}">
						<div>
							<img src="{{ asset('img/' ~ produit.image) }}" alt="{{ produit.description }}" width="350" height="350">
						</div>
						<h2 class="description_bijoux_index">{{ produit.description }}</h2>
						<div class="promo1">OFFRE EXCEPTIONNELLE</div>
						<div class="promo2">PROMO</div>
					</a>
				</div>
			{% endfor %}
		</div>

		<h3>Nos Montres Incontournables à prix Raisonnable
		</h3>
		{# 
																																								<div class="d-flex justify-content-around" class="block_watch">
																																			
																																			
																																									{% for produit in montres1 %}
																																										<div class="montresAcceuil">
																																											<div>
																																												<img src="{{ asset('img/' ~ produit.image) }}" alt="{{ produit.description }}" width="400" height="400">
																																											</div>
																																											<h2 style="width:400px;margin-left:20px;">{{ produit.description }}</h2>
																																			
																																			
																																										</div>
																																									{% endfor %}
																																			
																																								</div>
																																			
																																			
																																								<div class="d-flex justify-content-around" class="block_watch">
																																			
																																			
																																									{% for produit in montres2 %}
																																										<div class="montresAcceuil">
																																			
																																											<div>
																																												<img src="{{ asset('img/' ~ produit.image) }}" alt="{{ produit.description }}" width="400" height="400">
																																											</div>
																																											<h2 style="width:400px;margin-left:20px;">{{ produit.description }}</h2>
																																			
																																			
																																										</div>
																																									{% endfor %}
																																			
																																								</div> #}

		<div class="d-flex justify-content-around block_watch">

			{% for produit in montres1 %}
				<div class="montresAcceuil">
					<a href="{{ path('afficher_produit', {'id': produit.id}) }}">
						<div>
							<img src="{{ asset('img/' ~ produit.image) }}" alt="{{ produit.description }}" width="400" height="400">
						</div>
						<h2 class="description_montres_index">{{ produit.description }}</h2>
					</a>
				</div>
			{% endfor %}

		</div>

		<div class="d-flex justify-content-around block_watch">

			{% for produit in montres2 %}
				<div class="montresAcceuil">
					<a href="{{ path('afficher_produit', {'id': produit.id}) }}">
						<div>
							<img src="{{ asset('img/' ~ produit.image) }}" alt="{{ produit.description }}" width="400" height="400">
						</div>
						<h2 style="width:400px;margin-left:20px;color:black;">{{ produit.description }}</h2>
					</a>
				</div>
			{% endfor %}

		</div>


		<div class="d-flex justify-content-around logos_footer">


			{% for logo in logos %}
				<div class="block_footer">

					<div>
						<img src="{{ asset('img/' ~ logo.imageLogo) }}" width="" height="">
					</div>
					<h2>{{ logo.descriptionLogo}}</h2>

				</div>
			{% endfor %}

		</div>

 <div id="footer" class="block_footer1">
	<div class="block_footer2" >
			

		<div class="partie_finale">
		<div class="invisible_footer"></div>
			<a href="{{ path('app_a_propos_de_nous') }}">A Propos De Nous</a>

		</div>	

		<div class="partie_finale">
				<div class="contact_final">
					<h2>Nous Contacter</h2>
					<i class="fa-solid fa-envelope"></i>
					<i class="fa-solid fa-phone"></i>
				</div>

				<p>Nous contacter
														+33 6 51 48 90 88
														(Appel Non Surtaxé)
				</p>
				<p>ous pouvez nous rejoindre en nous envoyant un message sur notre boite Email suivante : chic_mademoiselle@gmail.com 
																																																																																															</p>

		
		</div>

		<div class="partie_finale">
	        <h2>Notre Livraison</h2>	
			<p>Toutes les commandes sont envoyées via Colissimo UPS ou Chronopost.
															Les commandes effectuées le samedi et le dimanche seront traitées le lundi suivant. <br>Livraison standard gratuite dès 49€ d'achat</p>

		</div>

		

	</div>

	<hr>
		<h4>Tous les droits sont réservés <i class="fa-solid fa-copyright"></i> -2024</h4>
</div>
		


	{% endif %}


{% endblock %}
